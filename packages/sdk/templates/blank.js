// Blank cart template — minimal starting point

function _init() {
    gfx.cls(0);
}

function _update(dt) {
    // Update logic here
}

function _draw() {
    gfx.cls(0);
    gfx.print("hello, dithr!", 4, 4, 7);
}
